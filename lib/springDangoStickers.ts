/** Sticker GIFs copied from ~/Downloads/表情包/表情包240 → public/creative/spring-dango-points/stickers */
export const SPRING_DANGO_STICKERS = Array.from({ length: 21 }, (_, i) => {
  const n = i + 1;
  return `/creative/spring-dango-points/stickers/${n}.gif`;
});
